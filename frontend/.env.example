# Only needed if the backend is deployed on a different origin than the frontend.
# In local dev this is left unset — Vite proxies /api to http://localhost:8000.
# VITE_API_BASE=https://your-backend.example.com
